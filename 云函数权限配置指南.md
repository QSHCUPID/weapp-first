# 云函数权限配置指南

## 问题描述

当小程序调用云函数时，出现以下错误：

```
Error: errCode: -501023 permission denied 
errMsg: RuleNotExists: rule for gardenUnion not exists
```

这是因为**云函数的安全规则未配置**，导致用户无法调用该云函数。

---

## 解决方案

### 方案一：通过微信开发者工具配置（推荐）

#### 步骤 1：打开云开发控制台

1. 打开微信开发者工具
2. 点击顶部菜单栏的"云开发"按钮
3. 进入云开发控制台

#### 步骤 2：配置云函数权限

1. 在云开发控制台左侧菜单，选择**"云函数"**
2. 找到 `gardenUnion` 云函数
3. 点击该云函数，进入详情页
4. 找到**"权限设置"**或**"安全规则"**选项卡
5. 配置权限规则（见下方配置示例）

#### 步骤 3：选择合适的权限配置

**选项 A：允许所有用户访问（适用于无需鉴权的场景）**

```json
{
  "invoke": true
}
```

**选项 B：仅允许登录用户访问（推荐）**

```json
{
  "invoke": {
    "auth.uid": {
      "$ne": null
    }
  }
}
```

**选项 C：自定义权限规则**

```json
{
  "invoke": {
    "$or": [
      {
        "auth.uid": {
          "$ne": null
        }
      },
      {
        "action": {
          "$in": ["getCurrentUser", "getFlowers"]
        }
      }
    ]
  }
}
```

#### 步骤 4：保存并生效

1. 点击"保存"按钮
2. 等待配置生效（通常立即生效）
3. 重新测试小程序功能

---

### 方案二：通过云开发网页控制台配置

#### 步骤 1：登录云开发控制台

1. 访问：https://console.cloud.tencent.com/tcb
2. 使用微信扫码登录
3. 选择对应的云开发环境

#### 步骤 2：配置云函数权限

1. 左侧菜单选择**"云函数"**
2. 找到 `gardenUnion` 云函数
3. 点击**"权限设置"**
4. 添加安全规则（参考上方配置示例）
5. 点击**"保存"**

---

### 方案三：通过代码配置（高级）

在云函数目录下创建 `config.json` 文件：

**文件路径：** `cloudfunctions/gardenUnion/config.json`

**内容：**

```json
{
  "permissions": {
    "openapi": []
  },
  "triggers": [],
  "security": {
    "invoke": true
  }
}
```

然后重新部署云函数：

```bash
# 进入云函数目录
cd cloudfunctions/gardenUnion

# 重新上传云函数
# 方法1：使用微信开发者工具右键云函数 -> 上传并部署
# 方法2：使用 CLI 工具
```

---

## 权限规则详解

### 基本语法

```json
{
  "invoke": <规则表达式>
}
```

### 常用规则示例

#### 1. 允许所有人访问

```json
{
  "invoke": true
}
```

#### 2. 禁止所有人访问

```json
{
  "invoke": false
}
```

#### 3. 仅允许已登录用户

```json
{
  "invoke": {
    "auth.uid": {
      "$ne": null
    }
  }
}
```

#### 4. 基于用户 openid 限制

```json
{
  "invoke": {
    "auth.openid": "特定的openid"
  }
}
```

#### 5. 允许特定操作（基于请求参数）

```json
{
  "invoke": {
    "action": {
      "$in": ["getCurrentUser", "getFlowers", "inputUser"]
    }
  }
}
```

#### 6. 组合条件（AND）

```json
{
  "invoke": {
    "$and": [
      {
        "auth.uid": {
          "$ne": null
        }
      },
      {
        "action": {
          "$in": ["markOwned", "markGrowing"]
        }
      }
    ]
  }
}
```

#### 7. 组合条件（OR）

```json
{
  "invoke": {
    "$or": [
      {
        "auth.uid": {
          "$ne": null
        }
      },
      {
        "action": "getCurrentUser"
      }
    ]
  }
}
```

---

## 针对 gardenUnion 云函数的推荐配置

根据该云函数的实际使用场景，推荐以下配置：

### 配置方案（推荐）

```json
{
  "invoke": true
}
```

**说明：**
- 允许所有用户调用该云函数
- 云函数内部已通过 `openid` 和数据库中的 `role` 字段进行权限控制
- 适合该项目的使用场景

### 高级配置方案（可选）

如果希望在云函数层面增加额外的权限控制：

```json
{
  "invoke": {
    "$or": [
      {
        "action": {
          "$in": ["getCurrentUser", "getFlowers", "getTempUrl"]
        }
      },
      {
        "$and": [
          {
            "auth.uid": {
              "$ne": null
            }
          },
          {
            "action": {
              "$in": [
                "getAllUsers",
                "inputUser",
                "markOwned",
                "unmarkOwned",
                "markGrowing",
                "unmarkGrowing",
                "addFlower",
                "deleteFlower"
              ]
            }
          }
        ]
      }
    ]
  }
}
```

**说明：**
- 查询类操作（getCurrentUser、getFlowers、getTempUrl）允许所有人访问
- 写操作需要用户登录（auth.uid 不为空）

---

## 验证配置是否生效

### 方法 1：通过小程序测试

1. 重启小程序（完全关闭后重新打开）
2. 触发调用云函数的操作
3. 检查是否还有 `-501023` 错误

### 方法 2：查看控制台日志

1. 打开微信开发者工具的"调试器"
2. 查看 Console 输出
3. 确认不再有权限错误

### 方法 3：查看云函数调用统计

1. 进入云开发控制台
2. 选择"云函数" -> "监控统计"
3. 查看 `gardenUnion` 的调用情况
4. 确认调用成功率提升

---

## 常见问题

### Q1：配置后仍然报错？

**解决方法：**
1. 确认配置已保存
2. 清除小程序缓存（开发者工具 -> 清除缓存）
3. 完全重启开发者工具
4. 检查云函数是否已部署最新版本

### Q2：如何查看当前的权限配置？

**方法：**
1. 云开发控制台 -> 云函数 -> gardenUnion
2. 点击"权限设置"选项卡
3. 查看当前规则

### Q3：配置错误会影响其他云函数吗？

**答案：** 不会。每个云函数的权限规则是独立的，修改 `gardenUnion` 的权限不会影响其他云函数。

### Q4：如何临时禁用权限检查？

**方法（仅用于开发调试）：**
```json
{
  "invoke": true
}
```

**警告：** 生产环境应配置严格的权限规则！

---

## 错误信息收集功能

代码已添加完善的错误处理机制，当遇到权限错误时会：

1. **自动收集客户信息**，包括：
   - 错误代码和详细信息
   - 小程序 AppID 和版本
   - 设备系统和微信版本
   - 时间戳和错误堆栈

2. **友好的错误提示**：
   - 向用户展示清晰的错误说明
   - 提供"复制信息"按钮，便于反馈
   - 包含解决方案指引

3. **开发者日志**：
   - 完整错误信息打印到控制台
   - 便于开发者快速定位问题

---

## 安全建议

### 生产环境配置建议

1. **最小权限原则**：只授予必要的权限
2. **基于用户身份验证**：要求用户登录后才能访问敏感操作
3. **定期审查权限**：定期检查和更新权限规则
4. **日志监控**：启用云函数调用日志，及时发现异常

### 开发环境配置建议

1. **快速迭代**：可以使用 `"invoke": true` 简化开发
2. **记录问题**：遇到权限问题时记录详细日志
3. **逐步收紧**：开发完成后逐步添加权限限制

---

## 总结

**必做步骤：**

1. ✅ 进入云开发控制台
2. ✅ 找到 `gardenUnion` 云函数
3. ✅ 配置权限规则为：`{"invoke": true}`
4. ✅ 保存并测试

**配置完成后：**
- ✅ 错误消失
- ✅ 云函数正常调用
- ✅ 小程序功能恢复正常
- ✅ 错误信息自动收集（如再次出现问题）

---

## 技术支持

如果按照以上步骤操作后仍有问题，请提供以下信息：

1. 小程序 AppID
2. 云开发环境 ID
3. 错误发生的完整截图
4. 控制台的完整错误日志
5. 当前的权限配置截图

通过小程序的"复制信息"功能，可以快速获取所需的诊断信息。
