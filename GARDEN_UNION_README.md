# 🌸 花园世界工会助手 - 部署指南

## 功能概览

- ✅ 工会名称显示：蕊香阁
- ✅ 用户录入系统（绑定微信，支持超级管理员）
- ✅ 花朵搜索（模糊搜索）
- ✅ 视图过滤（已拥有/未拥有/待培育）
- ✅ 竞赛分过滤和排序
- ✅ 花朵列表展示（图片、名称、拥有人数、类型、拥有者标签、分数）
- ✅ 一键标记拥有花朵
- ✅ 花朵录入功能（支持图片压缩上传）

## 部署步骤

### 1. 上传云函数

1. 打开微信开发者工具
2. 进入 `cloudfunctions/gardenUnion` 目录
3. 右键点击 `gardenUnion` 文件夹
4. 选择「上传并部署：云端安装依赖」
5. 等待部署完成

### 2. 创建数据库集合

在微信云开发控制台，创建以下三个数据库集合：

| 集合名称 | 说明 |
|---------|------|
| `gu_users` | 用户表 |
| `gu_flowers` | 花朵表 |
| `gu_ownerships` | 拥有记录表 |

### 3. 设置数据库权限

为这三个集合设置权限为「所有用户可读，仅创建者可写」或「自定义安全规则」。

建议的安全规则示例：

```json
{
  "read": true,
  "write": true
}
```

（生产环境建议设置更严格的规则）

### 4. 配置超级管理员

有两种方式设置超级管理员：

#### 方式一：在云函数代码中配置（推荐先试用）

编辑 `cloudfunctions/gardenUnion/index.js`，找到 `ADMIN_OPENIDS` 数组，添加你的 openid：

```javascript
const ADMIN_OPENIDS = [
  '你的openid在这里',  // 添加你的 openid
]
```

**如何获取你的 openid？**
1. 在小程序中随便点击一下，触发云函数
2. 去云开发控制台的「云函数」→「日志」中查看
3. 找到日志中的 `OPENID: xxx` 字段，复制那个 xxx

#### 方式二：在数据库中直接设置

1. 先运行一次小程序，让系统自动创建你的用户记录
2. 去云开发控制台的「数据库」→ `gu_users` 集合
3. 找到你的记录，把 `role` 字段改成 `"admin"`

### 5. 测试运行

1. 在微信开发者工具中编译运行
2. 在工具大集合首页应该能看到「花园世界工会助手」入口
3. 点击进入，开始使用！

## 数据库结构说明

### gu_users（用户表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `openid` | String | 微信用户的 openid（管理员录入的用户为空字符串） |
| `role` | String | 角色：`admin`（管理员）或 `user`（普通用户） |
| `gameName` | String | 游戏昵称 |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

### gu_flowers（花朵表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `name` | String | 花朵名称 |
| `score` | Number | 竞赛分数 |
| `type` | String | 花朵类型（元宝活动、花灵活动等） |
| `image` | String | 花朵图片的云文件ID |
| `createdBy` | String | 创建者的 openid |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

### gu_ownerships（拥有记录表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `openid` | String | 拥有者的 openid |
| `userId` | String | 拥有者在 gu_users 表中的 _id |
| `flowerId` | String | 花朵在 gu_flowers 表中的 _id |
| `createdAt` | Date | 记录时间 |

## 菜单配置

在 `miniprogram/config.js` 中可以控制菜单的显示/隐藏：

```javascript
gardenUnion: {
  enabled: true,  // 改为 false 可以隐藏入口
  name: '花园世界工会助手',
  ...
}
```

## 常见问题

### Q: 云函数部署失败怎么办？
A: 检查以下几点：
- 确保已经开通了云开发
- 检查云函数目录下是否有 `package.json`
- 尝试右键选择「上传并部署：不安装依赖」，然后在云端手动安装

### Q: 图片上传失败？
A: 确保云开发的存储服务已开通，并且有足够的存储空间。

### Q: 如何添加更多超级管理员？
A: 两种方式：
1. 在云函数的 `ADMIN_OPENIDS` 数组中添加更多 openid
2. 在数据库 `gu_users` 表中把相应用户的 `role` 改为 `admin`

## 后续优化建议

- [ ] 添加花朵编辑和删除功能
- [ ] 添加导出报表功能
- [ ] 添加花朵详情页面
- [ ] 添加统计分析图表
- [ ] 添加消息通知功能
- [ ] 添加批量导入花朵功能

---

祝大帅使用愉快！✨ 有问题随时找小美～ 💕
