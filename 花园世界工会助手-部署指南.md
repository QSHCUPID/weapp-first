# 🌸 花园世界工会助手 - 部署指南

## 📋 功能概览

- ✅ 工会名称显示：蕊香阁
- ✅ 用户录入与更新（绑定微信，支持修改昵称）
- ✅ 超级管理员系统（通过数据库 role 字段）
- ✅ 花朵搜索（模糊搜索）
- ✅ 双模式过滤：工会拥有 / 个人拥有
- ✅ 花朵三种状态：未拥有 / 培育中 / 已拥有（互斥）
- ✅ 花朵图片存储（云存储 + 临时链接）
- ✅ 超级管理员删除花朵功能
- ✅ 菜单可配置化

---

## 🚀 部署步骤

### 1. 上传云函数

1. 打开微信开发者工具
2. 进入 `cloudfunctions/gardenUnion` 目录
3. 右键点击 `gardenUnion` 文件夹
4. 选择「上传并部署：云端安装依赖」
5. 等待部署完成

### 2. 创建数据库集合

在微信云开发控制台，创建以下三个数据库集合：

| 集合名称 | 说明 |
|---------|------|
| `gu_users` | 用户表 |
| `gu_flowers` | 花朵表 |
| `gu_ownerships` | 拥有记录表 |

### 3. 设置数据库权限

为这三个集合设置权限为「所有用户可读，仅创建者可写」或「自定义安全规则」。

建议的安全规则示例：

```json
{
  "read": true,
  "write": true
}
```

（生产环境建议设置更严格的规则）

### 4. 设置第一个超级管理员

由于移除了硬编码的管理员列表，需要在数据库里手动设置第一个管理员：

#### 方法一：先运行小程序，再修改角色（推荐）

1. 先运行一次小程序，让系统自动创建你的用户记录
2. 打开微信云开发控制台
3. 进入「数据库」→ `gu_users` 集合
4. 找到你的用户记录
5. 把 `role` 字段的值改成 `"admin"`

#### 方法二：直接在云开发控制台添加

1. 打开微信云开发控制台
2. 进入「数据库」→ `gu_users` 集合
3. 点击「添加记录」
4. 填入以下内容：
   ```json
   {
     "openid": "你的openid",
     "role": "admin",
     "gameName": "admin",
     "createdAt": {"$date": "2026-04-10T00:00:00.000Z"},
     "updatedAt": {"$date": "2026-04-10T00:00:00.000Z"}
   }
   ```

**如何获取你的 openid？**
- 在小程序里随便点一下，触发云函数
- 去云开发控制台的「云函数」→「日志」里查看
- 找到日志里的 `OPENID: xxx` 字段，复制那个 xxx

### 5. 后续添加其他管理员

只要在 `gu_users` 表里把相应用户的 `role` 字段改成 `"admin"` 就可以啦！

---

## 📊 数据库结构说明

### gu_users（用户表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `openid` | String | 微信用户的 openid（管理员录入的用户为空字符串） |
| `role` | String | 角色：`admin`（管理员）或 `user`（普通用户） |
| `gameName` | String | 游戏昵称 |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

### gu_flowers（花朵表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `name` | String | 花朵名称 |
| `score` | Number | 竞赛分数 |
| `type` | String | 花朵类型（元宝活动、花灵活动等） |
| `image` | String | 花朵图片的云文件ID（fileID） |
| `createdBy` | String | 创建者的 openid |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

### gu_ownerships（拥有记录表）

| 字段 | 类型 | 说明 |
|-----|------|------|
| `_id` | String | 自动生成的记录ID |
| `openid` | String | 微信用户的 openid（管理员操作时为空） |
| `userId` | String | 指向 gu_users._id |
| `flowerId` | String | 指向 gu_flowers._id |
| `status` | String | 状态：`owned`（已拥有）或 `growing`（培育中） |
| `createdAt` | Date | 记录时间 |

---

## 🎯 功能使用说明

### 1. 用户绑定游戏昵称

- 首次进入：点击「录入」按钮，输入游戏昵称
- 已录入后：点击「录入」按钮，可以修改游戏昵称
- 修改昵称时会检查是否被其他用户占用

### 2. 超级管理员功能

- 选择游戏昵称：点击右上角的游戏昵称区域，弹出用户选择器
- 录入新用户：点击「录入」按钮，可以录入任意游戏昵称
- 操作用户状态：选择用户后，可以为其标记/取消拥有/培育
- 删除花朵：只有超级管理员能看到 🗑️ 按钮，可以删除花朵
- 选择 admin 昵称时：不显示「个人拥有」过滤

### 3. 过滤模式

#### 🏢 工会拥有模式
- 过滤选项：全部 / 已拥有 / 未拥有
- 按所有人是否拥有来统计
- 不显示操作按钮

#### 👤 个人拥有模式
- 过滤选项：全部 / 已拥有 / 培育中 / 未拥有
- 按当前选择的用户来统计
- 显示操作按钮（🌱 和 ✔）

### 4. 花朵状态（互斥）

- ❌ **未拥有**：两个按钮都不亮
- 🌱 **培育中**：种花按钮点亮（橙色）
- ✔ **已拥有**：打勾按钮点亮（绿色）
- 三种状态互斥，只能有一种

---

## 📝 菜单配置

在 `miniprogram/config.js` 中可以控制菜单的显示/隐藏：

```javascript
gardenUnion: {
  enabled: true,  // 改为 false 可以隐藏入口
  name: '花园世界工会助手',
  ...
}
```

---

## 🖼️ 图片处理说明

参考了「棠棠成长日记」的图片处理方式：

1. **上传时**：
   - 图片上传到云存储（路径：`garden-union-flowers/`）
   - 数据库只存储 `fileID`（云文件ID）

2. **读取时**：
   - 调用云函数 `getTempUrl` 获取临时访问链接
   - 用临时链接显示图片

---

## ⚠️ 注意事项

1. **第一个管理员必须手动设置**（在数据库里把 role 改成 admin）
2. **删除花朵会同时删除相关的拥有记录**
3. **花朵名称不能重复**，录入时会检查
4. **游戏昵称不能重复**，录入/更新时会检查
5. **云函数部署后才能使用**所有功能

---

## 💡 后续优化建议

- [ ] 添加花朵编辑功能
- [ ] 添加批量导入花朵功能
- [ ] 添加导出报表功能
- [ ] 添加花朵详情页面
- [ ] 添加统计分析图表
- [ ] 添加消息通知功能

---

## 🆘 常见问题

### Q: 云函数部署失败怎么办？
A: 检查以下几点：
- 确保已经开通了云开发
- 检查云函数目录下是否有 `package.json`
- 尝试右键选择「上传并部署：不安装依赖」，然后在云端手动安装

### Q: 图片上传失败？
A: 确保云开发的存储服务已开通，并且有足够的存储空间。

### Q: 如何添加更多超级管理员？
A: 在 `gu_users` 表里把相应用户的 `role` 字段改成 `"admin"` 即可。

### Q: 小程序里看不到「花园世界工会助手」入口？
A: 检查 `miniprogram/config.js` 里的 `gardenUnion.enabled` 是否为 `true`。

---

祝大帅使用愉快！✨ 有问题随时找小美～ 💕
